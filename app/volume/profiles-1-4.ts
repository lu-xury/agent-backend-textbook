import type { ChapterProfile, TopicSeed } from "./types";

const t = (
  title: string,
  definition: string,
  mechanism: string,
  agentExample: string,
  decision: string,
  misconception: string,
  experiment: string,
): TopicSeed => ({ title, definition, mechanism, agentExample, decision, misconception, experiment });

export const profiles1to4: ChapterProfile[] = [
  {
    number: 1,
    title: "编程与代码基础",
    why: "如何读懂、修改并验证一段跨越模型、工具与数据库的后端代码，而不是只让 AI 生成看似合理的语法",
    project: "一个可以创建 run、调用模型并执行工具的最小 Agent Runtime",
    boundary: "外部表示进入系统后必须先被解析成可信领域类型，业务规则与基础设施实现分离",
    invariant: "任何退出路径都不会泄漏资源，非法状态不能被提交，错误保留原因并到达正确的处理者",
    evidence: "调用栈、类型检查结果、资源计数、结构化错误、单元测试和最小复现",
    failure: "代码在正常输入下通过，却在异常、取消或对象提前销毁时留下后台任务、半成品状态和无法解释的 500",
    languageNotes: {
      python: "用类型标注和运行时校验弥补动态性，用上下文管理器管理资源，并为 asyncio Task 建立父作用域",
      cpp: "用值语义、RAII、智能指针和 expected/异常表达所有权与失败，避免悬空引用和未定义行为",
      rust: "用所有权、借用、Result、enum 与 trait 排除非法状态，不用 clone、unwrap 或全局锁逃避设计",
    },
    topics: [
      t("值、引用与所有权", "变量名并不等于数据本身，赋值和传参会依据语言的值模型复制、移动或共享对象", "先画对象与引用关系，再判断修改是否可见、对象何时销毁以及谁负责释放", "工具调用参数在异步任务启动后被原请求修改，导致审计记录与真实执行参数不一致", "跨异步和线程边界时优先传递不可变快照或显式所有权，避免依赖共享可变对象", "看见相同变量名就认为三种语言有相同复制语义", "分别制造浅拷贝、悬空引用和所有权移动案例，观察编译期与运行期差异"),
      t("类型、解析与非法状态", "类型不仅描述存储格式，还应表达业务允许的状态集合", "外部字符串先解析为 DTO，再校验领域规则，最后构造无法表示非法组合的领域对象", "模型返回字符串形式的 tool arguments，代码直接转发给 shell，绕过范围与权限检查", "在边界做运行时校验，在核心层使用更窄类型；错误信息区分格式错误与业务冲突", "有类型标注或 JSON Schema 就不需要运行时验证", "设计 RunStatus、ToolName 和 RetryCount 类型，并用非法输入证明约束有效"),
      t("控制流、调用栈与副作用", "控制流说明下一步执行哪里，数据流说明值如何变化，副作用则改变函数外部世界", "从入口沿调用链标出分支、早返回、异常跳转、await 与写库或网络调用", "权限检查发生在写数据库之后，随后虽然返回 403，但未授权记录已经创建", "让校验和授权位于不可逆副作用之前，并用事务或幂等协议保护无法原子完成的步骤", "返回错误就意味着前面的操作自动回滚", "给 handler 画正常、校验失败、写库失败和客户端取消四条时序线"),
      t("函数契约与模块边界", "函数是关于输入、输出、错误和副作用的契约，模块是一组共同变化的责任", "高层用例依赖稳定接口，第三方 SDK 与框架对象留在外围适配器", "AgentService 直接返回供应商 SDK 的流事件，换模型后前端、日志和测试全部被迫修改", "先定义内部事件和错误模型，再为每家供应商编写适配器；抽象应对应真实变化轴", "把代码拆成更多文件就自动获得清晰架构", "将一个混合 handler 拆为协议翻译、用例和模型适配器，并用 fake 单测用例"),
      t("错误分类与传播", "错误处理的目标是保留原因、选择责任者并给调用方稳定语义，而不是把异常隐藏", "业务拒绝、暂时性依赖故障、结果未知和程序缺陷采用不同映射、重试与告警策略", "工具超时被 catch-all 转成空字符串，模型继续推理并把缺失结果当成真实事实", "只捕获能够恢复或翻译的错误，保留 cause、run_id 和阶段；未知结果不得伪装成功", "所有异常最终返回 500 就算统一错误处理", "构造四类错误并验证 HTTP 状态、日志字段、重试策略和用户消息各不相同"),
      t("资源管理与生命周期", "文件、连接、锁、线程和任务都有取得与释放，生命周期必须覆盖所有使用者", "资源句柄绑定作用域，早返回、异常和取消都通过 finally、RAII 或 Drop 归还", "后台任务捕获请求级数据库会话，handler 结束后连接已归还却仍被继续使用", "资源应由最长使用它的明确作用域拥有；后台任务自行获取连接并在自身结束前释放", "垃圾回收会及时关闭所有外部资源", "让操作在持有连接时抛错和取消，观察连接池占用是否回到基线"),
      t("迭代器、生成器与流式处理", "迭代抽象允许按需产生和消费数据，从而把内存占用与总数据量解耦", "生产者每次交付一个元素，消费者施加背压；结束、错误和取消也是流的一部分", "多个工具结果先拼成一个巨大字符串再截断，峰值内存触发 OOM 且丢失结果边界", "大结果流式写入受控存储，内存只保留窗口、摘要和引用，并明确最大元素与总预算", "用了生成器就自然拥有背压和取消", "生成十万条工具日志，对比列表聚合与异步流的峰值内存和取消延迟"),
      t("同步、异步与结构化并发", "异步让等待 I/O 的任务让出执行权，结构化并发要求子任务归属于可等待和可取消的父作用域", "事件循环调度就绪任务；父任务收集结果、传播 deadline，并在退出前收割所有子任务", "并行工具用 fire-and-forget 启动，用户取消后仍运行，异常只变成未处理警告", "独立 I/O 可并发，CPU 工作离开事件循环；所有子任务必须有所有者、上限和汇合点", "async 函数里的任何代码都不会阻塞", "并发启动两个可控工具，分别测试一方失败、总超时、取消和父任务提前退出"),
      t("依赖注入与设计原则", "依赖注入是从外部提供对象所需协作者，使核心逻辑不负责选择和创建具体实现", "业务层定义最小接口，组合根创建数据库、模型、时钟和策略，再注入用例对象", "测试为了替换模型 SDK 修改全局变量，多个测试并行时相互污染", "优先构造函数注入；接口围绕调用方需求设计，避免巨型万能接口和隐藏 service locator", "使用框架 DI 容器就自动遵守 SOLID", "为模型、时钟和 ID 生成器注入 fake，写出无需网络且可重复的用例测试"),
      t("工程工具、Git 与系统化调试", "可复现构建、静态检查、版本控制和调试流程把个人代码变成可协作工程", "先保存事实和最小复现，再提出单一假设、设计对照实验，最后用回归测试封住根因", "AI 一次生成数十个文件，功能、重构和依赖升级混在同一提交，故障无法二分定位", "锁定依赖、缩小提交、检查 diff；格式化、lint、类型检查和测试成为固定质量门禁", "只要 CI 绿色就证明设计正确", "故意引入一个跨文件错误，用日志、栈、git bisect 思路和最小测试完成定位报告"),
    ],
  },
  {
    number: 2,
    title: "计算机与 Linux 基础",
    why: "如何从进程、内存、文件、端口和信号等系统事实解释“本机正常、服务器失败”",
    project: "运行在 Linux 服务器和容器中的 Agent API 与工具 Worker",
    boundary: "应用进程只拥有操作系统授予的 CPU、内存、文件描述符、网络和权限",
    invariant: "进程在资源上限、信号和重启下仍能给出可诊断状态，并在宽限期内安全停止或恢复",
    evidence: "PID、启动命令、退出码、内核日志、RSS、文件描述符、监听 socket、用户身份和容器限制",
    failure: "团队只看应用异常，却忽略 OOM、SIGKILL、端口占用、权限和命名空间，最终把系统故障误判成业务 Bug",
    languageNotes: {
      python: "理解解释器进程、GIL、multiprocessing、signal 和文件描述符，不让阻塞调用卡住事件循环",
      cpp: "理解线程、栈堆、动态库、core dump 与系统调用，用 RAII 和 stop_token 管理系统资源",
      rust: "理解 Send/Sync、panic、Tokio runtime 与系统调用边界，用 tracing 和 Drop 观察资源生命周期",
    },
    topics: [
      t("CPU、内存、磁盘与网络", "程序的每一步最终消耗计算、工作内存、持久存储或数据传输，四者延迟与容量差异巨大", "先识别瓶颈资源，再看等待队列、吞吐和峰值，而不是只看平均利用率", "模型响应很慢，团队盲目加 CPU，但进程实际在等待网络且连接池已排队", "依据测量结果优化关键资源；为峰值和尾延迟留余量，避免凭资源名称猜测", "CPU 使用率不高就说明服务器足够空闲", "分别制造 CPU 循环、内存聚合、磁盘同步写和慢网络，比较 top、iostat 思路与延迟"),
      t("进程、线程与协程", "进程拥有独立地址空间，线程共享进程内存，协程由用户态运行时协作调度", "调度单元越轻，共享越多；隔离、通信成本和故障爆炸半径也随之变化", "一个不可信工具在线程中崩溃，导致承载其他用户 run 的整个 API 进程退出", "可信 I/O 用协程，受控 CPU 用线程或进程池，不可信执行优先独立进程或容器", "协程是更快的线程，线程又只是更轻的进程", "让相同任务用进程、线程和协程运行，记录隔离、取消、共享状态和吞吐差异"),
      t("虚拟内存、堆栈与 OOM", "进程看到的是虚拟地址空间，实际内存由页、映射、堆栈、分配器和内核共同管理", "瞬时峰值超过 cgroup 或系统限制时内核可直接杀进程，程序无法依赖 finally 收尾", "并行工具把结果同时驻留内存，容器达到 2GiB 后被 SIGKILL，宿主机仍显示有空闲", "限制并发和结果大小，大对象流式外置；监控峰值 RSS，并让任务可从 checkpoint 恢复", "RSS 上升一定代表代码内存泄漏", "逐步增大并发结果，观察 RSS、退出信号和容器限制，验证无异常日志的 OOM"),
      t("文件、目录与权限", "Linux 把文件和目录访问交给用户、组和权限位，目录权限还决定能否遍历和创建名称", "内核按进程有效身份逐层检查路径；容器内 UID 与宿主机文件所有者可能不一致", "Worker 能读取挂载目录却无法创建 checkpoint，团队直接 chmod 777 掩盖了身份配置错误", "先确定运行用户、所有者、父目录和最小权限；生产目录不授予不必要的全局写权限", "文件可读就意味着所在目录也可访问且文件可写", "创建不同用户和权限组合，预测读、写、删除与重命名结果，再用事实验证"),
      t("文件描述符、管道与标准流", "文件描述符是进程引用文件、socket、管道等内核对象的整数句柄", "open 或 accept 分配描述符，close 释放；达到上限后连日志文件和新连接都可能无法打开", "HTTP 客户端响应未关闭，长时间运行后出现 Too many open files，重启暂时恢复", "所有句柄绑定作用域，监控数量趋势；stdout/stderr 用于可收集日志，stdin 只在明确交互时读取", "垃圾回收最终会关闭描述符，所以泄漏只影响内存", "循环制造未关闭响应并观察描述符增长，再用上下文管理和 RAII 修复"),
      t("环境变量、工作目录与进程环境", "进程启动时继承参数、环境变量、当前目录、用户和资源限制，这些共同构成运行环境", "相对路径由当前目录解释，环境变量只有字符串且可能缺失，秘密不应被打印或散落读取", "服务从终端启动正常，systemd 启动却找不到配置，因为工作目录和 PATH 完全不同", "入口一次读取并校验配置，路径转绝对；运行单元显式声明目录、用户和必要环境", "服务器会继承开发终端里的所有变量和 PATH", "用干净环境和不同工作目录启动同一程序，记录失败并改成显式配置"),
      t("信号与优雅退出", "信号是内核向进程传递事件的机制，SIGTERM 可处理，SIGKILL 无法捕获", "收到 TERM 后服务先停止接单，通知任务取消，等待宽限期，再关闭连接池并退出", "部署直接发送 KILL，进行中的 tool call 留下外部副作用但状态仍是 running", "正常停止使用 TERM 和有限宽限期；所有长任务持有取消令牌，恢复流程处理被强杀情况", "捕获所有信号就能保证任何时候安全保存状态", "发送 TERM、INT 和 KILL，比较日志、析构、退出码和残留任务"),
      t("端口、监听地址与 socket", "端口标识主机上的网络端点，监听地址决定哪些网络接口接受连接", "bind 建立本地地址，listen 建立队列，accept 产生连接描述符；127.0.0.1 只在本机回环可达", "容器内 curl 成功，宿主机访问失败，因为应用只监听容器的 127.0.0.1", "服务容器通常监听 0.0.0.0，再用端口映射和防火墙控制外部可达范围", "0.0.0.0 是客户端应该访问的服务器地址", "分别监听回环和所有接口，用 ss 映射到 PID，并验证容器端口映射"),
      t("容器、命名空间与 cgroup", "容器是受命名空间隔离并由 cgroup 限制资源的普通进程集合，不是轻量虚拟机魔法", "进程在自己的 PID、网络和挂载视图中运行，资源上限可独立于宿主机", "监控宿主机内存正常，但 Agent 容器因 cgroup 上限 OOM；localhost 也指向错误命名空间", "排障先声明观察位置；明确挂载、网络、UID 和资源限制，不把容器内视图当宿主机事实", "进入容器后看到的 PID、localhost 和文件系统与宿主机完全相同", "比较宿主机与容器的 PID、路由、挂载和内存上限，画出边界"),
      t("系统化性能与故障排查", "系统排查是从症状到资源事实再到可证伪假设的过程", "先确定时间窗口和受影响请求，再沿服务、进程、资源、内核与容器边界逐层缩小", "Worker 偶发消失，团队反复升级依赖，实际内核日志早已记录 OOM kill", "优先收集不会改变现场的只读证据，建立基线和对照，修复后保留指标与复现测试", "应用日志没有异常就说明问题不在服务器", "用一个同时包含端口冲突、权限错误和内存峰值的故障环境完成证据报告"),
    ],
  },
  {
    number: 3,
    title: "网络与 HTTP",
    why: "如何把一次长时间模型请求拆成 DNS、连接、TLS、HTTP、代理、流式事件与取消等可验证阶段",
    project: "通过浏览器流式订阅 run、调用外部模型与工具 API 的 Agent 服务",
    boundary: "每一次跨网络调用都只有有限时间预算，响应、断连和重试不会自动等价于业务结果",
    invariant: "请求在重复、超时、断连和代理存在时仍具有稳定协议语义，副作用不会被无意执行两次",
    evidence: "DNS 结果、连接阶段耗时、HTTP 方法和状态、响应头、事件 ID、代理配置、trace 与下游请求标识",
    failure: "把网络错误统一叫作超时并对所有失败自动重试，最终制造重复写入、重试风暴和断开后仍持续计费的任务",
    languageNotes: {
      python: "用 httpx/asyncio 明确连接池、阶段超时、deadline、流式 Body 和取消传播",
      cpp: "用 Boost.Asio/Beast 理解 socket、executor、timer、TLS 与回调或协程对象生命周期",
      rust: "用 reqwest/hyper、Tower timeout 与 Stream 理解 Future、连接复用、Body 背压和取消",
    },
    topics: [
      t("IP、端口、域名与 DNS", "域名通过 DNS 解析为地址，IP 标识网络接口，端口标识主机上的具体服务端点", "解析、路由和端口监听是不同阶段；缓存、IPv4/IPv6 与容器 DNS 会改变实际目标", "模型域名在某个 Worker 解析到旧地址，只有部分实例请求失败", "记录解析结果和时间，设置合理缓存；不要把域名可解析等同于端口可连接", "能 ping 通域名就证明 HTTP 服务正常", "修改本地解析或启动双地址服务，分别验证 DNS、TCP 和 HTTP 故障"),
      t("TCP 连接、可靠字节流与连接池", "TCP 提供有序可靠字节流，但不理解 HTTP 消息，也不保证应用操作成功", "握手建立连接，序列号与确认实现重传；连接池复用连接并限制并发", "模型调用排队 20 秒，实际不是供应商生成慢，而是客户端连接池已耗尽", "分别设置连接超时、池等待和读取超时；容量依据下游并发与尾延迟估算", "一个 TCP write 对端一定对应一次 read，连接成功等于请求成功", "限制连接池为 2 并发发起 10 个慢请求，观察池等待与读取耗时"),
      t("HTTPS、TLS 与证书", "TLS 在传输层之上认证服务身份并加密完整性保护，HTTPS 是运行在 TLS 上的 HTTP", "客户端验证证书链、主机名和有效期，再协商密钥；代理可能终止并重新建立 TLS", "开发环境关闭证书验证后上线，代理配置错误导致请求悄悄连到错误内部服务", "生产永不全局关闭验证；管理信任根、证书轮换和 SNI，区分客户端与服务端错误", "使用 HTTPS 就自动证明响应内容可信且无业务风险", "创建自签名证书，比较可信根、主机名错误和过期证书的失败"),
      t("HTTP 请求、响应与语义", "HTTP 消息由方法、目标、头和可选 Body 组成，状态码表达协议层结果", "中间件和代理按方法、Header 与缓存语义处理，业务结果仍需由响应模型明确描述", "取消 run 使用 GET 并被浏览器预取触发，任务在用户未点击时停止", "安全方法不产生业务副作用；POST、PUT、PATCH、DELETE 按资源语义和幂等性选择", "状态码 200 就代表业务一定完成，POST 永远不能重试", "用 curl 构造不同方法、Content-Type 和条件请求，观察路由和缓存行为"),
      t("REST 资源与 API 契约", "REST 风格用资源、表示和标准方法组织接口，契约包含请求、响应、错误与兼容策略", "协议 DTO 与领域模型分开，服务端对字段、分页、排序和状态转换给出稳定规则", "前端依赖供应商原始 tool_call 字段，模型升级后所有客户端同时损坏", "公开 API 只暴露内部稳定概念；新增可选字段向后兼容，破坏性变化使用版本策略", "路径看起来像名词就自动是良好 REST API", "为 run、message、tool-call 设计资源接口和统一错误体，并评审状态转换"),
      t("超时、deadline 与取消传播", "超时限制某一等待阶段，deadline 限制整条操作剩余时间，取消表达调用方不再需要结果", "入口预算逐层递减，下游不能各自重新获得完整时长；取消沿调用树传播", "浏览器已断开，API 停止写 SSE，但模型请求与工具仍在后台运行并计费", "为每层传递剩余 deadline 和取消信号；订阅断开是否取消 run 由产品契约决定", "给每个函数设置 60 秒超时就得到 60 秒总耗时", "串联三个慢服务，对比独立超时与共享 deadline，并测试断连传播"),
      t("重试、幂等与结果未知", "重试重新执行操作，幂等保证多次相同意图产生等价结果，结果未知表示可能已经成功", "只对暂时性且可安全重放的操作有限退避；写操作使用幂等键和结果查询", "创建 run 的响应在返回途中丢失，客户端重试后生成两个收费任务", "在意图入口生成稳定幂等键，服务端保存首次结果；未知结果先查询或对账", "网络报错说明服务器一定没收到请求", "在提交前后分别断开连接，验证幂等键阻止重复 run"),
      t("SSE 与流式 HTTP", "SSE 用 text/event-stream 在一个 HTTP 响应中发送带边界、类型和 ID 的文本事件", "服务端 flush 事件，代理不得缓冲；客户端用 Last-Event-ID 重连并重放缺口", "本地逐 token 输出，上线经过代理后 40 秒一次性显示", "定义事件 schema、心跳、重连和最终状态；持久化事件后再推送", "应用调用 flush 就保证浏览器一定实时收到", "实现六类 run 事件，经过代理验证首事件延迟和断线恢复"),
      t("WebSocket、长连接与心跳", "WebSocket 在握手后提供双向消息通道，需要应用自行管理连接身份、顺序和恢复", "连接状态保存在进程时，多实例扩容需要共享路由或外部事件层；心跳发现半开连接", "用户连接切到另一实例后无法收到原 Worker 的工具进度", "只有真正需要高频双向控制才选 WebSocket；业务状态独立持久化，连接只是订阅渠道", "WebSocket 比 SSE 更先进，所以 Agent 默认都应使用", "实现 ping/pong、断线重连和跨实例事件转发，观察连接状态与 run 状态差异"),
      t("代理、CORS 与端到端行为", "反向代理会终止连接、改写头、缓冲响应并施加超时；CORS 是浏览器对跨源读取的限制", "用户看到的协议行为由客户端、CDN、代理和应用共同决定，任一层默认值都可能改变结果", "应用测试通过，但 Nginx 60 秒超时关闭长 run，浏览器又因错误 CORS 无法读取失败体", "按路由配置缓冲、超时和头；CORS 精确允许可信来源，不用通配符搭配凭据", "CORS 错误说明服务器没有收到请求，代理只负责转发不会改变流", "用代理复现缓冲、超时和预检，记录每层请求与响应"),
    ],
  },
  {
    number: 4,
    title: "Web 框架与 API 开发",
    why: "如何让路由、校验、业务、存储与外部适配器各自承担清晰责任，使 API 可测试、可演进",
    project: "提供 run 创建、查询、取消、事件订阅和工具管理接口的 Agent API",
    boundary: "框架负责 HTTP 翻译，Service 负责用例，Repository 负责持久化，Adapter 负责外部方言",
    invariant: "任何入口都执行相同的认证、校验、授权和错误映射，业务层不依赖具体 HTTP 或供应商对象",
    evidence: "OpenAPI 契约、请求 ID、路由测试、用例单测、事务记录、统一错误体和生命周期日志",
    failure: "所有逻辑堆进 handler，导致事务跨越慢网络、测试只能启动整套服务、错误格式和权限规则在不同路由中漂移",
    languageNotes: {
      python: "用 FastAPI、Pydantic 与依赖注入组织 DTO 和用例，但避免把 Depends 与 ORM 对象渗入核心层",
      cpp: "用 Drogon 处理协议，借助明确接口和 RAII 控制连接、事务与异步回调生命周期",
      rust: "用 Axum、Tower、Serde 与 Tokio，把 extractor、middleware 和领域 Result 保持分层",
    },
    topics: [
      t("路由与协议翻译", "路由把方法和路径映射到入口，入口负责把 HTTP 表示翻译为用例输入与输出", "handler 读取 path、query、header 和 body，建立请求上下文后调用单一业务用例", "创建 run 的 handler 同时拼 prompt、查库、调用模型并写 SSE", "让 handler 薄而明确；业务层接收普通领域参数，不接收 Request 或框架 Response", "handler 行数少就一定分层良好", "把一个巨型路由拆成 DTO、用例和响应映射，并分别测试"),
      t("请求 DTO 与分层校验", "结构校验回答数据能否解析，领域校验回答操作在当前业务状态下是否合法", "框架先解析 DTO，Service 再根据数据库状态和调用主体检查业务不变量", "Pydantic 或 Serde 验证字段后直接执行高风险工具，未检查用户是否拥有目标 workspace", "格式、领域和授权错误分开处理；外部输入永远不因类型生成而自动可信", "Schema 通过就说明请求完全有效", "构造缺字段、越界、状态冲突和越权四类请求，验证不同错误"),
      t("Router、Service、Repository 分层", "分层按变化原因隔离协议、业务与数据访问，不是机械增加文件夹", "Router 调用 Service，Service 协调事务和策略，Repository 提供领域所需查询与写入", "Repository 返回 ORM 懒加载对象，连接关闭后 Service 才访问字段并报错", "跨层传稳定领域值；事务边界由用例控制，Repository 不隐藏远程调用", "每张表配一个 Repository 就自然获得良好架构", "实现 CreateRun 用例，用内存 Repository 单测并替换为 PostgreSQL"),
      t("依赖注入与组合根", "组合根在应用启动时创建具体依赖并注入对象图，其余代码只声明需要什么", "配置、连接池、模型客户端和策略在入口构造，按应用、请求或任务作用域管理", "请求级对象被单例 Service 持有，下一位用户复用了上一位用户身份", "显式声明生命周期；业务对象不从全局容器随时查找依赖", "使用框架 Depends 或 State 就无需思考作用域", "注入 fake 模型、固定时钟和临时 Repository，验证并发请求不串上下文"),
      t("中间件与请求上下文", "中间件在路由前后处理横切关注点，如请求 ID、认证、日志和指标", "中间件形成有顺序的链，必须保证异常、流式和取消路径也执行收尾", "日志中间件在响应对象创建时记录完成，SSE 实际运行十分钟却显示耗时 20ms", "只放真正横切且不改变业务语义的逻辑；流式完成时间以 Body 结束为准", "中间件适合实现任何跨路由复用的业务规则", "编写请求 ID 与计时中间件，分别测试普通响应、异常和 SSE"),
      t("统一错误模型", "错误模型把内部失败分类映射为稳定状态码、机器码、消息和追踪信息", "领域错误不依赖 HTTP，最外层异常处理器做协议映射并保留 cause 到日志", "数据库唯一约束被统一转成 500，客户端不断重试相同请求", "明确 400、401、403、404、409、422、429、503 等语义；不把内部栈暴露给用户", "只要返回 JSON 就算统一错误格式", "建立错误矩阵并让三种语言实现返回同一 contract test"),
      t("配置与应用生命周期", "配置在启动时读取、解析和校验，生命周期钩子按顺序创建与关闭共享资源", "启动失败应快速退出；关闭时停止接单、取消任务、等待宽限期再释放依赖", "服务已关闭数据库池，后台 Agent 任务仍尝试保存最后事件", "对象销毁顺序必须是创建顺序的逆依赖关系；不在业务模块随处读环境变量", "环境变量存在就无需配置类型和启动校验", "故意缺少配置、启动依赖失败和发送 TERM，验证日志与关闭顺序"),
      t("流式接口与客户端断开", "流式接口把响应生命周期延长到最后一个事件，断连只是传输状态，不等于业务取消", "生成器或 Stream 逐块产出，框架和代理传播背压与取消，最终状态单独持久化", "SSE 客户端关闭标签页后 handler 结束，但 run 仍需允许稍后重新订阅", "明确订阅与任务两个生命周期；高水位限制慢客户端占用资源", "返回 StreamingResponse 或 Body Stream 就完成全部流式设计", "实现慢客户端、断线重连和显式取消，比较资源与 run 状态"),
      t("OpenAPI、版本与兼容", "接口文档是机器可读契约，版本策略管理客户端与服务端独立演进", "从 DTO 生成 schema，再用 contract test 验证真实响应；破坏性变化有迁移窗口", "服务端把字段 tool_calls 改名为 actions，旧前端静默丢失工具进度", "优先新增可选字段和新端点；错误码与枚举也属于兼容面", "自动生成 OpenAPI 就保证文档与业务完全一致", "保存上一版本契约，运行兼容检查并模拟旧客户端"),
      t("文件、Webhook 与优雅关闭", "上传、下载和 Webhook 都是普通 HTTP，但涉及大 Body、外部回调、安全验证与异步处理", "上传流式落盘并限制大小；Webhook 先验签和去重再入队；关闭期间拒绝新任务并排空", "Webhook 响应超时后供应商重发，系统重复启动昂贵 run", "边界先验证身份和幂等键，快速确认接收；重工作异步执行并记录状态", "第三方只会发送一次 Webhook，文件大小由客户端声明可信", "发送重复、乱序和伪造 Webhook，并上传超限文件测试资源上限"),
    ],
  },
];
